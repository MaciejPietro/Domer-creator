import { EditorPage } from '@/2d/EditorPage';
import { AppShell } from '@mantine/core';

const Homepage = () => {
    return (
        <AppShell>
            <EditorPage />
        </AppShell>
    );
};

export default Homepage;
